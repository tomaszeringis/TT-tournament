version: '3.8'
services:
  web:
    build: .
    command: streamlit run app/main.py
    ports:
      - "8501:8501"
    volumes:
      - .:/app
  api:
    build: .
    command: uvicorn api.server:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
